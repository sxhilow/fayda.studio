---
interface Props{
    id?: string,
    class?: string
}

const {
    id,
    class: className
} = Astro.props;
---

<section id={id} class:list={["max-w-7xl mx-auto py-20 space-y-8 lg:space-y-16 px-5 xl:px-0", className]}>
    <slot/>
</section>