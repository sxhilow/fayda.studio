---
import Pill from "@ui/Pill.astro";
import Rocket from "@icons/Rocket.svg";
import Button from "@ui/Button.astro";
import ContactForm from "@ui/ContactForm.astro";
import ProjectBadge from "@icons/ProjectBadge.svg";
---

<section class="border-b border-neutral-12">
    <div class="flex max-lg:flex-col max-w-7xl mx-auto w-full h-full">
        <div
            class="flex-1 flex flex-col justify-center items-start gap-8 w-full border-b lg:border-b-0 lg:border-r border-neutral-12 py-16 lg:pr-16 self-stretch px-5 relative"
        >
            <div>
                <Pill label="Trusted by businesses & startups">
                    <Rocket slot="icon" />
                </Pill>
            </div>

            <div class="space-y-2">
                <h1 class="text-brand-surface text-display-lg">
                    We Design and Build Modern
                    <span
                        class="inline-block font-secondary text-brand-red tracking-normal font-semibold animate-fade-blur-in"
                        id="rotate-text"
                    >
                        Websites
                    </span>
                </h1>

                <p class="text-brand-surface text-body-md">
                    Stop losing customers to slow, outdated websites. We create
                    custom-coded, high-performance digital experiences that look
                    great on every device.
                </p>
            </div>

            <div class="flex max-sm:flex-col w-full gap-4">
                <Button
                    href="/services"
                    label="Explore Our Services"
                    class="hero-cta-btn"
                    data-label="Explore Our Services"
                    data-destination="/services"
                />
                <Button
                    href="/case-studies"
                    label="View Case Studies"
                    iconPosition="left"
                    variant="outline"
                    class="hero-cta-btn"
                    data-label="View Case Studies"
                    data-destination="/case-studies"
                />
            </div>

            <div
                class="absolute lg:-right-10 lg:top-1/2 lg:-translate-y-1/2 max-lg:bottom-0 max-lg:left-1/2 max-lg:-translate-x-1/2 max-lg:translate-y-1/2 group"
            >
                <ProjectBadge class="w-20 h-20 lg:rotate-0 max-lg:rotate-90 group-hover:scale-105 transition-all duration-300 " />
            </div>
        </div>

        <div class="flex-1 flex flex-col h-full w-full py-16 lg:pl-16 px-5">
            <!-- <div
                class="flex-3/4 flex flex-col justify-center items-center md:pt-32 border-b border-neutral-12"
            >
                <div class="py-5 px-5 xl:px-20 w-full">
                    <div class="overflow-x-auto w-full">
                        <table
                            class="w-full min-w-[350px] bg-linear-to-b from-brand-blue/10 to-blue-900/10 rounded-2xl overflow-hidden"
                        >
                            <caption
                                class="text-brand-surface text-body-lg font-medium"
                            >
                                What <span class="text-brand-blue">Set us</span> Apart
                            </caption>

                            <thead>
                                <tr class="text-brand-surface">
                                    <th class="font-semibold px-4 py-4"
                                        >Features</th
                                    >
                                    <th
                                        class="font-semibold text-brand-red px-4 py-4"
                                        >Fayda.studio</th
                                    >
                                    <th class="font-semibold px-4 py-4"
                                        >Typical Agencies</th
                                    >
                                </tr>
                            </thead>

                            <tbody class="bg-brand-blue/5 rounded-2xl">
                                {
                                    comparisonTableData.map((row) => (
                                        <tr class="text-brand-surface text-xs sm:text-sm border-b border-blue-900 last:border-b-0">
                                            <td class="py-2 px-4">
                                                {row.feature}
                                            </td>

                                            <td class="py-2 px-4">
                                                <div class="flex items-center gap-2">
                                                    <Check />
                                                    <span>{row.us}</span>
                                                </div>
                                            </td>

                                            <td class="py-2 px-4">
                                                <div class="flex items-center gap-2">
                                                    <Close />
                                                    <span>{row.them}</span>
                                                </div>
                                            </td>
                                        </tr>
                                    ))
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <div
                class="flex-1/4 flex justify-center items-center gap-2 group py-8 px-5"
            >
                <Button
                    href="/#work"
                    label="Explore our latest Projects"
                    variant="ghost"
                    class="text-neutral-7 font-secondary text-h3 animate-bounce group-hover:[animation-play-state:paused] group-hover:text-brand-surface transition-all duration-150 gap-4"
                >
                    <Right slot={"icon"} />
                </Button>
            </div> -->

            <ContactForm />
        </div>
    </div>
</section>

<script>
    document.addEventListener("astro:page-load", () => {
        document.querySelectorAll(".hero-cta-btn").forEach((btn) => {
            btn.addEventListener("click", () => {
                window.posthog?.capture("hero_cta_clicked", {
                    label: (btn as HTMLElement).dataset.label,
                    destination: (btn as HTMLElement).dataset.destination,
                });
            });
        });
    });
</script>

<script is:inline>
    const words = ["Websites", "Products", "Platforms", "Brands"];
    let currentIndex = 0;
    const textEl = document.getElementById("rotate-text");

    function rotate() {
        textEl.classList.remove("animate-fade-blur-in");
        textEl.classList.add("animate-fade-blur-out");

        setTimeout(() => {
            currentIndex = (currentIndex + 1) % words.length;
            textEl.innerText = words[currentIndex];

            textEl.classList.remove("animate-fade-blur-out");
            textEl.classList.add("animate-fade-blur-in");
        }, 600); // This delay matches the animation duration in tailwind global.css
    }

    setInterval(rotate, 2000);
</script>
